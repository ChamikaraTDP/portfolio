import { Source_Sans_3, Mulish, Lora } from "next/font/google";

export const sourceSans = Source_Sans_3({ subsets: ["latin"] });
export const mulish = Mulish({ subsets: ["latin"] });
export const lora = Lora({ subsets: ["latin"] });
